<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data class="ApSimpleAudioDialogTitleBinding">

        <import type="com.pine.audioplayer.R"></import>

        <variable
            name="playType"
            type="com.pine.audioplayer.bean.ApPlayListType" />

        <variable
            name="mediaCount"
            type="Integer" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="@dimen/dp_20">

        <ImageView
            android:id="@+id/play_type_iv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="@dimen/dp_15"
            android:paddingTop="@dimen/dp_5"
            android:src="@{playType.imageResId}"></ImageView>

        <TextView
            android:layout_width="@dimen/dp_0"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{@string/ap_sad_title(playType.name,mediaCount)}"
            android:textColor="@color/dark_black_gray"
            android:textSize="@dimen/sp_18"></TextView>

        <ImageView
            android:id="@+id/clear_iv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="@dimen/dp_15"
            android:paddingTop="@dimen/dp_5"
            android:src="@mipmap/res_ic_clear"></ImageView>
    </LinearLayout>
</layout>